#include <stdio.h>

int main()
{
    int n = 6;
    for (int i = 0; i < n; i++)
    {
        printf("The value of i is %d\n");
    }
    return 0;
}

/*
What is for loop?
A for loop is a control flow statement that allows code to be executed repeatedly based on a given Boolean condition. The for loop is typically used when the number of iterations is known beforehand. The syntax of a for loop in C is as follows:  
for (initialization; condition; increment/decrement) {
    // code to be executed
}
*/
