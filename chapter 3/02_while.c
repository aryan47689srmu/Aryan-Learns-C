#include <stdio.h>

int main()
{
    int i = 0;
    while (i < 10)
    {
        printf("Happy birthday!\n");
        i = i + 1;
    }
    return 0;
}


/*
what is while loop?
A while loop is a control flow statement that allows code to be executed repeatedly based on a given Boolean condition. The while loop can be thought of as a repeating if statement. The syntax of a while loop in C is as follows:        
while (condition) {
    // code to be executed
}
*/
