#include <stdio.h>

int main()
{
    int a = 10;
    int b = 5;
    int c = a / b;
    printf("The value of a is %d and b is %d and quotient is %d", a, b, c);

    return 0;
}

/*
                + (Addition)
                - (Subtraction)
                * (Multiplication)
                / (Division)
                % (Modulus)   -:   oprator gives the remainder of the division of two numbers
*/
