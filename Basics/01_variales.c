#include<stdio.h>
int main() 
{
 int a; // variable declaration
a = 10; // variable initialization
 printf("The value of a is: %d\n", a); // printing the value of variable a
    return 0;
}


/*
what is a variable?
A variable is a named storage location in memory that can hold a value. It is used to store data that can be manipulated and accessed throughout a program. Variables have a specific data type that determines the kind of value they can hold, such as integers, floating-point numbers, characters, etc. In C, you must declare a variable before using it, specifying its data type and name. You can also initialize a variable at the time of declaration or assign a value to it later in the program. Variables are essential for storing and manipulating data in any programming language, including C.   
*/
