#include <stdio.h>
int main()
{
    int a;
    scanf("%d", &a);                      // taking input from the user and storing it in variable a
    printf("The value of a is: %d\n", a); // printing the value of variable a
    return 0;
}
