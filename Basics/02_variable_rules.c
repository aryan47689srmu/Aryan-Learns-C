#include <stdio.h>
int main()
{
    int aryan;    // This is valid because variable names can contain letters and can start with a letter.
    int _aryan;   // This is valid because variable names can start with an underscore.
    int aryan123; // This is valid because variable names can contain numbers, but cannot start with a number.
    int Aryan;    // This is valid because variable names are case-sensitive, so 'Aryan' is different from 'aryan'.
    return 0;
}
